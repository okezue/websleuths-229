name: Atlas adaptive closure check

on:
  push:
    branches: [atlas-proof-check-temp]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check:
    if: github.actor != 'atlas-proof-check-bot'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: atlas-proof-check-temp
      - name: Install Lean
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Check actual adaptive-ROM closure
        working-directory: atlas-check
        shell: bash
        run: |
          lake update
          lake exe cache get
          mkdir -p .lake/build/lib/lean/AtlasCheck
          : > CLOSURE_STATUS.latest.txt
          : > closure-kernel.latest.log
          status=0
          for m in AdaptiveData AdaptiveCausality AdaptiveGame AdaptiveCounting AdaptiveExtraction AdaptiveTape AdaptiveSampleEquiv AdaptiveClosure; do
            echo "===== $m =====" >> closure-kernel.latest.log
            lake env lean AtlasCheck/$m.lean -o .lake/build/lib/lean/AtlasCheck/$m.olean >> closure-kernel.latest.log 2>&1
            c=$?
            if [ $c -eq 0 ]; then echo "PASS $m" >> CLOSURE_STATUS.latest.txt; else echo "FAIL $m" >> CLOSURE_STATUS.latest.txt; status=$c; fi
          done
          echo $status > CLOSURE_EXIT.latest.txt
      - name: Persist report
        working-directory: atlas-check
        run: |
          git config user.name atlas-proof-check-bot
          git config user.email actions@users.noreply.github.com
          git add CLOSURE_STATUS.latest.txt CLOSURE_EXIT.latest.txt closure-kernel.latest.log
          if ! git diff --cached --quiet; then
            git commit -m '[skip ci] Persist adaptive closure diagnostics'
            git push origin HEAD:atlas-proof-check-temp
          fi
      - name: Enforce
        working-directory: atlas-check
        run: test "$(cat CLOSURE_EXIT.latest.txt)" = 0
